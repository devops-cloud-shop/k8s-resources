apiVersion: v1
kind: Service
metadata:
  name: service-lb
spec:
  type: loadbalancer
  selector:
    component: shipping
    environment: prod
    purpose: loadbalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      NodePort: 30005